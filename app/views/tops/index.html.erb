
<div class= "container">
  <header class="header"></header>
    <nav class = "navbar navbar-default clearfix">
    <nav class = "navbar navbar-default">

      <div class = "container-fluid">
        <div class = "navbar header">
          <button type = "button" class = "navbar-toggle collapsed" data-toggle = "collapse" data-target = "#gnavi">
            <span class = "sr-only">Happy Talk</span>
            <span class = "icon-bar"></span>
            <span class = "icon-bar"></span>
            <span class = "icon-bar"></span>
          </button>
          <a class = "navbar-brand">Happy Talk</a>
        </div>
        <div id = "gnavi" class = "collapse navbar-collapse">
          <ul class = "nav navbar-nav">
            <% if logged_in? %>
              <li><%= link_to "Go to Happy Talk",happy_talks_path %></li>
            <% else %>
              <li><%= link_to "Go to Happy Talk", new_session_path %></li>
            <% end %>
              <li><%= link_to "Make Account", new_user_path %></li>
            <% if logged_in? %>
              <li><%= link_to "Logout", session_path(current_user.id,'logout'), method: :delete %></li>
            <% else %>
              <li><%= link_to "Login", new_session_path %></li>
            <% end %>

          </ul>
        </div>
      </div>
     </nav>

    <section class="main-image" "container">
      <%= image_tag 'happy_talk_top.png', :class => "top-image" %>
    </section>

    <section class="about" "container">
      <h1>About Happy Talk</h1>
      <p>
        夢を言葉に。幸せを形に。<br>
      </p>
      <p>
        夢の大きさに比例して、あなたの人生は大きく成長します。<br>
        実現したhappyも、まだ実現していないhappyも、あなたのhappyをシェアしよう。
      </p>
    </section>

    <section class="function" "container-fluid">
      <div class="function-images" "row" "function-image-color" >
        <%= link_to new_contact_path do  %>
          <div class="col-md-4 col-sm-12" >
            <i class="far fa-envelope fa-fw icon" ></i>
            <p>Contact</p>
          </div>
          <% end %>

        <% if logged_in? %>
          <%= link_to session_path(current_user.id,'logout'), method: :delete do %>
            <div class="col-md-4 col-sm-12">
              <i class="fas fa-sign-out-alt fa-fw icon"></i>
              <p>Logout</p>
            </div>
          <% end %>
        <% else %>
          <%= link_to new_session_path do %>
            <div class="col-md-4 col-sm-12">
              <i class="fas fa-sign-in-alt fa-fw icon"></i>
              <p>Login</p>
            </div>
          <% end %>
        <% end %>

        <a href = "/users/new" class="col-md-4 col-sm-12"><i class="far fa-address-book fa-fw icon"></i>
          <p>Make account</p>
        </a>
      </div>
    </section>

    <footer class="footer">
      <p class="footer-text">Happy Talk since 1985</p>
    </footer>
  </div>
